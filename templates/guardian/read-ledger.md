# Read Ledger

Required when SOURCE_FAITHFUL or final strong claim is made, evidence will be reused across sessions, another Agent/reviewer must audit reads, or source coverage is disputed.

Declared ReadSet hash:

Actual reads:

- path:
  section:
  purpose:
  matches ReadSet: yes/no

Extra reads:

- path:
  reason:
  cold-read trigger:

Skipped reads:

- path:
  reason:

ReadSet violations:

- none
